import AdminSupport from '../../../src/views/AdminSupport';

export const metadata = { title: 'Admin Support', robots: { index: false, follow: false } };

export default function Page() {
  return <AdminSupport />;
}
